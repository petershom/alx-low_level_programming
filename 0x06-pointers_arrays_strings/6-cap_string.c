#include "main.h"

/**
 * cap_string - capitalizes all words of a string
 * @s: string
 * 
 * Return: address s 
 */

char *cap_string(char *s)
{
	int i = 0, j;
	char a[] = " \t\n,;.!?\"(){}";

	while (*(s + i))
	{
		if (*(s + i) >= 'a' && *(s + i) <= 'z')
		{
			if (i == 0)
				*(s + i) -=
		}
	}
}

