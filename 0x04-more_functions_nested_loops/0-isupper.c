#include "main.h"

/**
 * main -  a function that checks for uppercase character
 *
 * Return: 0
 */

int _isupper(int ups);

main(void)
{
	if ((ups >= 'A') && (ups <= 'Z'))
	{
		return (1);
	}
	else
	{
		return (0);
	}


}
