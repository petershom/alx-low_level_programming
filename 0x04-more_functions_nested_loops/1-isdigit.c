#include "main.h"

/**
 * main - a function that checks for a digit (0 through 9)
 *
 * Return: 1, 0
 */

int _isdigit(int i);

main(void)
{
	if ((i >= 0) && (i <= 9))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
