#include "main.h"

/**
 * main - a function that reverses a string
 *
 * Return: Always 0
 */

void rev_string(char *s);
{
        int len = strlen(s);

        while(*(s + len)
                len++;

        return (len);
}

