#include "main.h"
#include <stdio.h>

/**
 * main - a function that prints half of a string, followed by a new line
 *
 * Return: Always 0
 */

void puts_half(char *str);
{
        int len = 0;

        while(*(s + len) != '\0')
                len++;

        return (len);
}
